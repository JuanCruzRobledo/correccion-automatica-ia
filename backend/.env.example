# ============================================
# CONFIGURACIÓN DEL BACKEND
# ============================================
# Este archivo es específico del backend
# Copiar como .env: cp .env.example .env

# ============================================
# BASE DE DATOS (MongoDB)
# ============================================
# Para desarrollo local:
# MONGODB_URI=mongodb://localhost:27017/correcion-automatica

# Para producción (MongoDB Atlas):
MONGODB_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/correcion-automatica

# ============================================
# SERVIDOR
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# AUTENTICACIÓN JWT
# ============================================
# Generar: openssl rand -hex 32
JWT_SECRET=tu-secreto-super-seguro-cambiar-en-produccion
JWT_EXPIRES_IN=7d

# ============================================
# ENCRIPTACIÓN
# ============================================
# Clave de encriptación (32 bytes hex = 64 caracteres)
# Generar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=tu-encryption-key-de-64-caracteres-hex-cambiar-en-produccion

# ============================================
# N8N - WEBHOOKS (Solo para IA)
# ============================================
# Webhook para generar rúbrica desde PDF
N8N_RUBRIC_WEBHOOK_URL=https://tu-servidor.n8n.example/webhook/rubrica

# Webhook para corrección individual con Gemini AI (nuevo - usado por batch)
N8N_INDIVIDUAL_GRADING_WEBHOOK_URL=http://localhost:5678/webhook/corregir-individual

# URL del backend (usado internamente por n8n para descargar archivos)
BACKEND_URL=http://localhost:5000

# ============================================
# CORS
# ============================================
CORS_ORIGIN=http://localhost:5173

# ============================================
# NOTAS
# ============================================
# - Los archivos de entregas se guardan en: backend/uploads/submissions/
# - NO se requiere Google Drive para almacenamiento
# - Solo n8n se usa para corrección con Gemini AI (no para carpetas/archivos)
